'use client';

import { LocaleSwitch } from '../../components/LocaleSwitch';

export const Footer = () => {
  return (
    <footer className="flex justify-center py-2">
      <LocaleSwitch />
    </footer>
  );
};
