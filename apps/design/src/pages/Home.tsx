import { FC } from 'react';
import { FormattedMessage } from 'react-intl';

export const Home: FC = () => (
  <FormattedMessage defaultMessage="These are the pages used to design and debug the components" />
);
